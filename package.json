{
  "name": "VatsakZinin",
  "homepage": "https://DiareMK.github.io/VatsakZinin",
  "dependencies": {
    "jquery": "^3.7.1",
    "bootstrap": "^5.3.0"
  },
  "scripts": {
    "build": "npm run build:styles && scripts build",
    "build:styles": "node_modules/.bin/sass src/styles.scss build/styles.css",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
